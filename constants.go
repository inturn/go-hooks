package main

const hookPath = ".git/hooks/"
const cachePath = hookPath + ".cache/"

const preCommitHook = "pre-commit"
const prePushHook = "pre-push"

const pushName = "push"
const commitName = "commit"
